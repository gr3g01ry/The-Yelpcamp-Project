<%if(success && success.length){%>
    <div class="alert alert-success alert-dismissible fade show w-75" role="alert">
        <strong><%=success%></strong>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
<%}%>

<%if(error && error.length){%>
    <div class="alert alert-warning alert-dismissible fade show  w-75 mx-auto" role="alert">
        <strong><%=error%></strong>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
<%}%>

{# can have only one message with many params
    
    [{'success':'OK It is a success'},
    {'error':'Ho no boy'},
    {'info':'You must be informed'},....
    ]
    add just take the key to setting the class alert
    #}